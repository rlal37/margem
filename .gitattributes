# Normalize line endings to LF for all text files (Prettier and CI expect LF).
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
